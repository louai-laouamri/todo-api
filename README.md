A REST API project for managing Todo tasks, built with Django and Django REST Framework.
Supports full CRUD operations with input validation, proper HTTP status codes, and error handling.
deployed it PythonAnywhere and configured using environment variables for flexibility across different environments.
